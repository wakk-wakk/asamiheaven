# Plan: Restore page.tsx Content

## Problem
The current `src/app/page.tsx` is damaged - most content was replaced with placeholder comments like `/* your hero content stays unchanged */`. Only 497 lines remain instead of the original ~961 lines.

## Solution
Restore the original page.tsx content from git, then make a surgical fix to lines 954-958 to properly close the CTA section.

## Steps

### Step 1: Restore original from git
```bash
git checkout HEAD -- src/app/page.tsx
```
This will restore the full ~961 line file that was working before your edit.

### Step 2: Verify build passes
```bash
npm run build
```
Build should pass after restore.

### Step 3: If there was a specific JSX error at lines 954-958 mentioned in your plan (malformed `</section>`), fix only that specific issue - the closing tags around lines 954-958:
- Current (broken): Missing proper closing `</div>` tags before `</section>`, then incorrectly nested extra `</div>` tags
- Fix: Ensure proper div closure order: `</div>` (flex) → `</div>` (content) → `</div>` (glass container) → `</div>` (max-w) → `</section>` (cta)

## Current Broken Lines 954-958 (after restore):
```
954:                 </Button>
955:                </div>
956:             </div>
957:           </div>
958:         </div>
959:       </section>
```

## Expected Fixed Lines 954-959:
```
954:                 </Button>
955:               </div>
956:             </div>
957:           </div>
958:         </div>
959:       </section>
```
Key fix: Line 958 should be `</div>` (closing the div from line 913 or 926), with proper indentation matching context.