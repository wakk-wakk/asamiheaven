# Plan: Fix WhatsApp and Viber Contact Buttons

## Requirements
1. **WhatsApp (Primary)** - must always work
   - URL: `https://wa.me/639319972797`
   - Open in new tab (`target="_blank"`)
   - Works on both desktop (WhatsApp Web) and mobile (app)

2. **Viber (Secondary)** - mobile-focused
   - URL: `viber://chat?number=639319972797`
   - Works best on mobile devices
   - May not work on desktop unless Viber is installed

3. **Device Detection**:
   - Mobile: show both buttons
   - Desktop: highlight WhatsApp (Viber optional)

4. **UI Requirements**:
   - Add note: "Viber works best on mobile"
   - Use brand colors: WhatsApp = green (#25D366), Viber = purple (#7360F2)
   - Add hover effects and smooth transitions
   - Make buttons responsive

5. **Placement**: Footer (Follow Us section), Contact page, Hero section

---

## Files to Modify

### 1. Footer (`src/components/layout/footer.tsx`)
- Add WhatsApp button with green brand color
- Update Viber button with purple brand color
- Add device detection to show/hide Viber on desktop
- Add note "Viber works best on mobile" next to Viber button

### 2. Contact Page (`src/app/(public)/contact/page.tsx`)
- Same changes as footer - update WhatsApp/Viber buttons with brand colors and device detection

### 3. Hero Section (`src/app/page.tsx`)
- Update Viber button to use `viber://chat?number=639319972797` format
- Keep WhatsApp as primary with green styling
- Optional: Add note about Viber being mobile-only

---

## Implementation Steps

### Step 1: Add device detection hook
Create a reusable function to detect if user is on mobile:
```typescript
const isMobile = () => {
  if (typeof window === 'undefined') return false
  return /Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent)
}
```

### Step 2: Update Footer
- Add `whatsappValue` state for phone number
- Add WhatsApp button with brand color `#25D366`
- Update Viber button with brand color `#7360F2`
- Show both on mobile, only WhatsApp on desktop (or show Viber with note on desktop)
- Add note: "Viber works best on mobile"

### Step 3: Update Contact Page
- Apply same styling and behavior as footer

### Step 4: Test and verify
- Verify WhatsApp works on desktop and mobile
- Verify Viber works on mobile
