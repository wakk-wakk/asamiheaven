# Fix JSX Closing Tag Error in page.tsx

## Problem
The build fails with error: `./src/app/page.tsx:959:7 - Expected '</', got 'ident'`

This is caused by mismatched/malformed closing tags at the end of the file.

## Current Broken Structure (Lines 955-961)
```
954:                </div>
955:             </div>
956:           </div>
957:         </div>
958: </section>    <- MALFORMED - missing indentation, should be indented
959:             </>
960:           )
961:         }
```

## Root Cause Analysis
The file uses a ternary pattern:
- Line 410: `return (<div className="animate-fade-in">` - opens root div
- Line 613: `) : (<>` - opens fragment for : branch  
- Lines 614-907: Multiple sections (therapists, services, reviews, CTA)
- Line 958: Should close `<section>` from line 911
- Line 959: Should close fragment from line 614 with `</>`
- Line 960: `)` - closes ternary
- Line 961: `}` - closes function

The issue at line 958: `</section>` has no leading indentation, making it look malformed to the parser.

## Fix Plan

### Step 1: Verify and fix line 958
Replace:
```
958: </section>
```
With proper indentation:
```
958:       </section>
```

### Step 2: Verify line 959 closes fragment
Line 959 should already be `</>` - if not, fix it.

### Step 3: Ensure lines 960-961 are correct
- Line 960: `)`
- Line 961: `}`

## Expected Result After Fix
```
954:                </div>
955:             </div>
956:           </div>
957:         </div>
958:       </section>
959:     </>
960:   )
961: }
```

## Alternative Fix (If Simple Edit Doesn't Work)

Option B: Replace lines 957-959 with:
```
957:       </section>
958:     </>
159:   )
160: }
```

This removes the nested divs and fixes the structure directly.

## Verification
After fix, run `npm run build` - should succeed.